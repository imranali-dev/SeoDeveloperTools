<!DOCTYPE html>
<html>
<head>
    <title>Delete Seller Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #333;
        }

        form {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            width: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            margin-bottom: 10px;
            font-weight: bold;
            color: #333;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .message {
            margin-top: 10px;
            text-align: center;
        }

        .error {
            color: red;
        }

        .success {
            color: green;
        }

        a {
            margin-top: 20px;
            text-decoration: none;
            color: #333;
        }
    </style>
    <style>
        /* Your existing styles remain unchanged */

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            color: #333;
        }

        form {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            width: 300px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            margin-bottom: 10px;
            font-weight: bold;
            color: #333;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .message {
            margin-top: 10px;
            text-align: center;
        }

        .error {
            color: red;
        }

        .success {
            color: green;
        }

        a {
            margin-top: 20px;
            text-decoration: none;
            color: #333;
        }
    </style>
</head>

<body>
    <h1>Delete Scammer Form by Id</h1>

    <form id="deleteForm" action="/alert/scammers/:id" method="POST">
        <label for="id">Enter ID to Delete:</label>
        <input type="text" id="id" name="id" required>
        <button type="button" onclick="deleteForm()">Delete</button>
    </form>

    <div id="message" class="message"></div>

    <a href="/">Back to Home</a>

    <script>
        async function deleteForm() {
            const id = document.getElementById('id').value;
            const messageDiv = document.getElementById('message');
            messageDiv.innerHTML = ''; // Clear previous messages

            try {
                const response = await fetch('/alert/scammers/' + encodeURIComponent(id), {
                    method: 'DELETE',
                });

                const data = await response.json();

                if (response.ok) {
                    messageDiv.innerHTML = `<p class="success">Table has been delted</p>`;
                } else {
                    messageDiv.innerHTML = `<p class="error">${error}</p>`;
                }
            } catch (error) {
                messageDiv.innerHTML = `<p class="error">Not Found deleting Table </p>`;
            }
        }
    </script>
</body>
</html>

